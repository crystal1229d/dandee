function ChecklistSection({ checklistId }: { checklistId?: string }) {
  // checklistId 가 있다면 => 해당 체크리스트 보여주기, 사용해제
  // checklistId 가 없다면 => 생성 OR 연결
  return <div>체크리스트</div>
}

export default ChecklistSection
